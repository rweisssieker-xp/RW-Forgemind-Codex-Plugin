interface:
  display_name: "Delegation Planning"
  short_description: "Plan bounded work with clear handoffs."
  default_prompt: "Use $delegation-planning to divide this outcome into evidence-backed tasks."
policy:
  allow_implicit_invocation: false
