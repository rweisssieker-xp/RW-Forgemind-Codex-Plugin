import { readFile } from 'node:fs/promises';
import { artifactStatePath } from './artifact-store.mjs';
import { invalidInput } from './errors.mjs';
import { writeJsonAtomic } from './io.mjs';
import { createInnovationPortfolio } from './innovation-portfolio.mjs';
import { createRadicalPortfolio } from './radical-product.mjs';
import { startChildAutopilot } from './autopilot.mjs';
const LIMIT = 3;
export async function discoverPortfolio({ workspace, goal, maxConcurrentCandidates = LIMIT }) { const [radical, innovation] = await Promise.all([createRadicalPortfolio({ workspace, goal }), createInnovationPortfolio({ workspace, goal })]); const candidates = dedupe([...radical.ideas.map((item) => card({ id: `radical-${item.id}`, source: 'radical', title: item.title, outcome: item.goal, interactionReplaced: item.interactionReplaced, tenXHypothesis: item.tenXHypothesis, aiCentrality: item.aiCore, moat: item.moat, killCondition: item.killCondition, score: item.score.total, evidenceBasis: item.evidenceBasis })), ...innovation.candidates.map((item) => card({ id: item.id, source: 'innovation', title: item.title, outcome: item.problemFocus, interactionReplaced: item.thesis, tenXHypothesis: `Create a category-level improvement for ${item.problemFocus} through ${item.title}.`, aiCentrality: item.archetype, moat: item.moat, killCondition: item.killCondition, score: item.score.total, evidenceBasis: item.evidenceBasis }))]).sort((a, b) => b.score - a.score || a.id.localeCompare(b.id)); const portfolio = { schemaVersion: 1, id: `portfolio-${Date.now().toString(36)}`, status: 'ready', goal: String(goal ?? '').trim() || radical.goal, maxConcurrentCandidates: positive(maxConcurrentCandidates, LIMIT), evidence: { basis: radical.evidence.basis, note: 'Candidates are repository-aware hypotheses. They are not market facts until supported by qualified evidence.' }, candidates, learningLedger: [], createdAt: new Date().toISOString(), updatedAt: new Date().toISOString(), artifactPath: '.codex-orchestrator/portfolio/latest.json', errors: [] }; await save(workspace, portfolio); return portfolio; }
export async function getPortfolio({ workspace }) { const portfolio = await load(workspace); return portfolio ? { schemaVersion: 1, status: portfolio.status, portfolio, errors: [] } : { schemaVersion: 1, status: 'missing', nextAction: 'Run portfolio discover first.', errors: [] }; }
export async function runPortfolio({ workspace }) { const portfolio = await required(workspace); const active = portfolio.candidates.filter((item) => ['running', 'ready'].includes(item.state)); for (const candidate of portfolio.candidates.filter((item) => item.state === 'queued')) { if (active.length >= portfolio.maxConcurrentCandidates) break; if (active.some((other) => conflict(candidate, other))) continue; const mission = await startChildAutopilot({ workspace, goal: candidate.outcome, autonomy: { maxActions: 10 }, id: `${portfolio.id}-${candidate.id}` }); candidate.state = 'ready'; candidate.missionId = mission.mission.id; candidate.startedAt = new Date().toISOString(); active.push(candidate); } portfolio.updatedAt = new Date().toISOString(); portfolio.status = active.length ? 'running' : 'ready'; await save(workspace, portfolio); return { schemaVersion: 1, status: portfolio.status, portfolio, activeCandidates: active.map((item) => ({ id: item.id, title: item.title, state: item.state, missionId: item.missionId, score: item.score })), errors: [] }; }
export async function stopCandidate({ workspace, id, reason }) { const portfolio = await required(workspace); const candidate = portfolio.candidates.find((item) => item.id === id); if (!candidate) throw invalidInput('FM_PORTFOLIO_CANDIDATE_UNKNOWN', `Unknown portfolio candidate: ${id}`); candidate.state = 'stopped'; candidate.stopReason = String(reason ?? 'Stopped by portfolio policy.'); portfolio.learningLedger.push({ candidateId: id, outcome: 'stopped', reason: candidate.stopReason, at: new Date().toISOString() }); await save(workspace, portfolio); return { schemaVersion: 1, status: 'passed', portfolio, candidate, errors: [] }; }
function card(input) { return { ...input, targetUser: 'Qualified user of the current repository application', metric: 'Independent completion, time-to-outcome, and reversal rate', guardrails: ['no critical defect', 'no accessibility regression', 'reversible change only'], rollback: { kind: 'revert-or-compensate', required: true }, conflictSurface: [`candidate:${input.id}`], state: 'queued', evidence: [] }; }
function dedupe(items) { const seen = new Set(); return items.filter((item) => { const key = `${item.interactionReplaced}|${item.outcome}`.toLowerCase(); if (seen.has(key)) return false; seen.add(key); return true; }); } function conflict(a, b) { return a.conflictSurface.some((item) => b.conflictSurface.includes(item)); } async function load(workspace) { try { return JSON.parse(await readFile(file(workspace), 'utf8')); } catch (error) { if (error.code === 'ENOENT') return null; throw error; } } async function required(workspace) { const portfolio = await load(workspace); if (!portfolio) throw invalidInput('FM_PORTFOLIO_MISSING', 'Run portfolio discover first.'); return portfolio; } async function save(workspace, value) { await writeJsonAtomic(file(workspace), value); } function file(workspace) { return artifactStatePath(workspace, 'portfolio', 'latest.json'); } function positive(value, fallback) { const number = Number(value); return Number.isInteger(number) && number > 0 ? number : fallback; }
