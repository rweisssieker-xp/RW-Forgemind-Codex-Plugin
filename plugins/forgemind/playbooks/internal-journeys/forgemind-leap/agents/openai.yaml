interface:
  display_name: "Leap"
  short_description: "Autonomous disruption from app or idea to tested MVP."
  default_prompt: "Use $forgemind-leap with its Zero-Input Default when no additional outcome is supplied."
policy:
  allow_implicit_invocation: false
