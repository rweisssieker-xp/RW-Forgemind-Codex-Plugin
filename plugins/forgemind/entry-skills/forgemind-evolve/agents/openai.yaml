interface:
  display_name: "Evolve"
  short_description: "Radically transform an existing app."
  default_prompt: "Use $forgemind-evolve to transform this existing app around a disruptive AI outcome."
policy:
  allow_implicit_invocation: false
