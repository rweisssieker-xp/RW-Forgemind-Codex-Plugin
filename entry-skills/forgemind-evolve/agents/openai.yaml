interface:
  display_name: "Evolve"
  short_description: "Radically transform an existing app."
  default_prompt: "Use $forgemind-evolve with its Zero-Input Default when no additional outcome is supplied."
policy:
  allow_implicit_invocation: false
