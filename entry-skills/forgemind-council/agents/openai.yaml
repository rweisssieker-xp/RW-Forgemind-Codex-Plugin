interface:
  display_name: "Council"
  short_description: "Evidence-labelled multi-perspective decisions."
  default_prompt: "Use $forgemind-council with its Zero-Input Default when no additional outcome is supplied."
policy:
  allow_implicit_invocation: false
