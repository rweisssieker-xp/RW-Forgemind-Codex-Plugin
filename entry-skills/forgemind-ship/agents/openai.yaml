interface:
  display_name: "Ship"
  short_description: "Build, test, and release a bounded outcome."
  default_prompt: "Use $forgemind-ship to implement, test, and prepare this outcome for release."
policy:
  allow_implicit_invocation: false
