interface:
  display_name: "Ship"
  short_description: "Build, test, and release a bounded outcome."
  default_prompt: "Use $forgemind-ship with its Zero-Input Default when no additional outcome is supplied."
policy:
  allow_implicit_invocation: false
