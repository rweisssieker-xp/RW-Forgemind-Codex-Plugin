# Debugging and Refactoring

Reproduce before fixing. Isolate the causal path, change only what resolves it, add focused regression coverage, and verify behavior. Refactor only when it directly supports the current outcome and preserves behavior.
