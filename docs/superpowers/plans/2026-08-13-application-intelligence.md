# Application Intelligence Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add repo-specific Twin, UX evolution, product lab, outcome memory, growth, and integration-mesh capabilities to ForgeMind Core.

**Architecture:** Six focused modules persist bounded artifacts below `.codex-orchestrator/` and enrich existing Portfolio, Autopilot, and Transform workflows without bypassing adapters or policy.

**Tech Stack:** Node.js 20+, ESM, node:test.

## Global Constraints

- Repository inferences and growth hypotheses are never market facts.
- No credentials, unconfigured remote operation, production mutation, external spend, or policy bypass.
- All entries are redacted, source-linked, project-local, and compatible with existing workflows.

---

### Task 1: Live Twin, UX Evolution, and Product Lab

**Files:** Create `src/application-twin.mjs`, `src/ux-evolution.mjs`, `src/product-lab.mjs`, and focused tests; modify Portfolio routing.

- [ ] Test deterministic Twin evidence labels, UX rollback requirements, and all six Product Lab perspectives.
- [ ] Implement project-local artifacts and Lab score/hold adjustment.
- [ ] Run focused tests.

### Task 2: Outcome Memory, Growth Loop, and Integration Mesh

**Files:** Create `src/outcome-memory.mjs`, `src/growth-loop.mjs`, `src/integration-mesh.mjs`, and focused tests.

- [ ] Test redaction, evidence requirements, growth claim boundaries, and held unconfigured/production integrations.
- [ ] Implement bounded artifacts and adapter-only mesh action plans.
- [ ] Run focused tests.

### Task 3: Entry points, CLI, dashboard, docs, mirrors, and release gate

**Files:** Add three skills, modify CLI/dashboard/docs, sync `plugins/forgemind`, and update journey tests.

- [ ] Test `twin`, `evolve-ui`, and `growth` commands plus dashboard rendering.
- [ ] Run `npm test`, `npm run ci`, strict Marketplace validation, commit, and push.
